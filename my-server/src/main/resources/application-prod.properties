# Server Settings (Production)
# ?? ?? ?? ? ?? ?? ??
server.address=0.0.0.0
server.port=8080

# Database (Production - H2 File)
# ??? Local? ??? ?? DB ?? (?? MySQL ??? ?? ??)
spring.datasource.url=jdbc:h2:file:./data/testdb

# JPA (Production)
spring.jpa.hibernate.ddl-auto=update